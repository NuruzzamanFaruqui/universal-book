# Copy to .env.local for local development. `npm run setup` does this for you.
#
# Where the website looks for the API. Leave it unset in a deploy and the
# production origin in src/lib/config.ts is used instead.
NEXT_PUBLIC_API_URL=http://localhost:8080
